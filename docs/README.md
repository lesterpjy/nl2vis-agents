# docs

The slides that go with this repository land here, as `presentation.pdf`.

Nothing else belongs in this directory. The design is explained in the repository's own
[README](../README.md); the eight SQL checks are demonstrated, query by query, in
[`tests/fixtures/sql-checks.md`](../tests/fixtures/sql-checks.md), which is also the test data for
`tests/test_sql_checks.py`.
